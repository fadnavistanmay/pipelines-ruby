# Ruby
# Package your Ruby application.
# Add steps that install rails, analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/ruby

pool:
  vmImage: 'Ubuntu 16.04'

steps:
- task: UseRubyVersion@0
  inputs:
    versionSpec: '>= 2.5'

- task: CacheBeta@1
  inputs:
    key: gems | $(Agent.OS) | pipelines-ruby.gemspec
    path: $(Pipeline.Workspace)/s/vendor/bundle
  displayName: Cache gems

- script: bundle install --path vendor/bundle
- script: |
    gem install bundler
    bundle install --path vendor/bundle --retry=3 --jobs=4
  displayName: 'bundle install'

- script: gem list
- script: ls -l $(Pipeline.Workspace)/s/vendor

- script: bundle exec rake
  displayName: 'bundle exec rake'

# - script: bundle exec rspec spec --format RspecJunitFormatter --out test_results/TEST-rspec.xml

- task: PublishTestResults@2
  condition: succeededOrFailed()
  inputs:
    testResultsFiles: '**/test-*.xml'
    testRunTitle: 'Ruby tests'
    
- task: PublishCodeCoverageResults@1
  inputs:
    codeCoverageTool: Cobertura
    summaryFileLocation: '$(System.DefaultWorkingDirectory)/**/coverage.xml'
    reportDirectory: '$(System.DefaultWorkingDirectory)/**/coverage'
